const $=id=>document.getElementById(id),c=$('c'),x=c.getContext('2d');let imgs=[],playing=false,raf=0,start=0;const state={duration:8};
function fitCanvas(){const [a,b]=$('ratio').value.split(':').map(Number);c.width=960;c.height=Math.round(960*b/a);draw(0)}
function ease(v){return v<.5?2*v*v:1-Math.pow(-2*v+2,2)/2}
function drawImageContain(img,cx,cy,maxW,maxH,scale=1,alpha=1,blur=0){const s=Math.min(maxW/img.width,maxH/img.height)*scale;x.save();x.globalAlpha=alpha;x.translate(cx,cy);x.filter=(blur?'blur('+blur+'px) ':'')+($('mono').checked?'grayscale(1) contrast(1.12)':'');x.drawImage(img,-img.width*s/2,-img.height*s/2,img.width*s,img.height*s);x.restore()}
function draw(t){const W=c.width,H=c.height,d=state.duration,n=Math.max(1,imgs.length);x.clearRect(0,0,W,H);const bg=x.createRadialGradient(W*.5,H*.46,0,W*.5,H*.46,W*.78);bg.addColorStop(0,'#292a30');bg.addColorStop(.5,'#101116');bg.addColorStop(1,'#020203');x.fillStyle=bg;x.fillRect(0,0,W,H);
if(imgs.length){const span=d/n,k=Math.min(n-1,Math.floor(t/span)),p=(t%span)/span,e=ease(p),cur=imgs[k],prev=k>0?imgs[k-1]:null,dir=k%2?-1:1,baseY=H*.51;if(prev)drawImageContain(prev,W*.5-dir*W*.62*(1-e),baseY,W*.7,H*.9,.7,.22,3);let cx=W*.5,cy=baseY,sc=.78;if($('motion').value==='zigzag'){cx=W*.5+dir*W*.20*Math.sin(e*Math.PI);cy=baseY-H*.025*Math.sin(e*Math.PI);sc=.74+.08*Math.sin(e*Math.PI)}else if($('motion').value==='push'){cx=W*.5-dir*W*.50*(1-e);sc=.70+.12*e}else{cx=W*.5+Math.sin(e*Math.PI*2)*W*.055;cy=baseY+Math.cos(e*Math.PI*2)*H*.018;sc=.78+.04*Math.sin(e*Math.PI)}drawImageContain(cur,cx,cy,W*.78,H*.9,sc,1,0);if(k<n-1&&p>.72)drawImageContain(imgs[k+1],W*.5+dir*W*.42*(1-(p-.72)/.28),baseY,W*.72,H*.86,.72,.55,1)}
x.strokeStyle='rgba(232,255,67,.72)';x.lineWidth=Math.max(1,W/900);x.beginPath();x.moveTo(W*.06,H*.77);x.bezierCurveTo(W*.30,H*.68,W*.60,H*.87,W*.94,H*.25);x.stroke();x.fillStyle='#fff';x.font='800 '+Math.max(18,W*.026)+'px system-ui';x.fillText($('title').value,W*.08,H*.82);x.fillStyle='#a8a8b0';x.font='500 '+Math.max(10,W*.012)+'px system-ui';x.fillText($('sub').value,W*.08,H*.87)}
function updateTime(t){$('time').textContent='00:'+String(Math.floor(t)).padStart(2,'0')+' / 00:'+String(state.duration).padStart(2,'0')}
function loop(now){if(!playing)return;const t=((now-start)/1000)%state.duration;draw(t);updateTime(t);raf=requestAnimationFrame(loop)}
$('ratio').onchange=fitCanvas;$('duration').oninput=()=>{state.duration=Math.max(2,Math.min(30,+$('duration').value||8));$('duration').value=state.duration;updateTime(0);draw(0)};
function loadFiles(list){imgs=[];$('thumbs').innerHTML='';[...list].slice(0,20).forEach(f=>{if(!f.type.startsWith('image/'))return;const i=new Image;i.onload=()=>{imgs.push(i);const t=document.createElement('img');t.src=i.src;t.className='thumb';t.title=f.name;$('thumbs').appendChild(t);draw(0);$('msg').textContent=imgs.length+' character'+(imgs.length===1?'':'s')+' loaded.'};i.src=URL.createObjectURL(f)})}
$('files').onchange=e=>loadFiles(e.target.files);const drop=$('drop');['dragenter','dragover'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',e=>loadFiles(e.dataTransfer.files));
$('preview').onclick=()=>{playing=!playing;if(playing){start=performance.now();$('preview').textContent='■ Stop Preview';raf=requestAnimationFrame(loop)}else{$('preview').textContent='▶ Preview';cancelAnimationFrame(raf)}};
$('render').onclick=()=>{if(!imgs.length){$('msg').textContent='Add at least one character image first.';return}if(playing){playing=false;cancelAnimationFrame(raf);$('preview').textContent='▶ Preview'}const d=state.duration,f=+$('fps').value,m=MediaRecorder.isTypeSupported('video/webm;codecs=vp9')?'video/webm;codecs=vp9':'video/webm',r=new MediaRecorder(c.captureStream(f),{mimeType:m,videoBitsPerSecond:16000000}),chunks=[];r.ondataavailable=e=>e.data.size&&chunks.push(e.data);r.onstop=()=>{const blob=new Blob(chunks,{type:'video/webm'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='character-effect-studio.webm';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),2000);$('msg').textContent='Render complete — WebM download started.'};r.start();$('msg').textContent='Rendering in your browser…';const z=performance.now();function q(now){const t=(now-z)/1000;if(t>=d){draw(d-.001);r.stop();return}draw(t);updateTime(t);requestAnimationFrame(q)}requestAnimationFrame(q)};
fitCanvas();updateTime(0);draw(0);